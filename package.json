{
  "name": "zooclaw-docs",
  "version": "0.0.1",
  "private": true,
  "description": "Public developer documentation for ZooClaw Managed Agents",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs --port ${DOCS_PORT:-5175}",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs --port ${DOCS_PORT:-5175}"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}
